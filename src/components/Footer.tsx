import { CardFooter } from "./ui/card";

export const Footer = () => {
  return (
    <CardFooter>
      <p>
        Use at your own risk. I am not liable for anything that ends up
        happening to you. This site does not use the official api, as its broke,
        but instead is a mirror of the StudentVue app. Funky stuff might happen
        so again don't get mad at me if something weird happens.
        <br />
        For questions contact me on Discord: srzhang
      </p>
    </CardFooter>
  );
};

export default Footer;
