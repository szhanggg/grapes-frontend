import { CardFooter } from "./ui/card";

export const Footer = () => {
  return (
    <CardFooter>
      <p>
        I am not liable for anything that ends up happening to you. This site
        does not use the official api, as its broke, but instead is a mirror of
        the StudentVue app.
        <br />
        For questions contact me on Discord: srzhang
      </p>
    </CardFooter>
  );
};

export default Footer;
